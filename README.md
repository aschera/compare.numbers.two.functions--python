# compare.numbers.two.functions--python
Compare two numbers that are inout by user.
Can only use numbers, 3,2 or 5.
Return 1 if number a is greater than number b
Return 0 is number a nd b are the same
Return -1 if number a is less than number b
